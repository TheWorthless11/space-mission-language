mission expression_test start
    // Expression operands
    int a;
    int b;
    float c;
    float d;

    a = 8 + 2;
    b = a - 3;
    c = 4.0 * 2.5;
    d = c / 2.0;

    // Mixed operations and precedence
    c = a + b * 2;
    d = (c + 1.0) / 3.0;
end
